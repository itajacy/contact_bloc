import 'package:flutter/material.dart';

class ContactsUpdatePage extends StatelessWidget {
  const ContactsUpdatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Contact Update'),
      ),
      body: Container(),
    );
  }
}
