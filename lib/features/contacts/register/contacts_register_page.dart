import 'package:flutter/material.dart';

class ContactsRegisterPage extends StatelessWidget {
  const ContactsRegisterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Register'),
      ),
      body: Container(),
    );
  }
}
